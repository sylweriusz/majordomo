# ── macOS ──────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# ── Swift Package Manager ──────────────────────────────
.build/
.swiftpm/

# ── Xcode (if the package is ever opened in Xcode) ─────
xcuserdata/
*.xcodeproj/xcuserdata/
DerivedData/

# ── Build output ───────────────────────────────────────
build/

# ── Marketing site ─────────────────────────────────────
# Deployed separately to Cloudflare Pages; not part of the app repo.
web_page/

# ── Rebuildable binaries / release artifacts ───────────
# Regenerated by scripts, kept out of git.
#   Helpers/whisper-*  ->  scripts/package-whisper-helper.sh (pinned whisper.cpp v1.8.6)
# A fresh clone must run package-whisper-helper.sh before build-app.sh.
# Source assets needed to build (icon PNGs, majordomo.pxd) stay tracked.
Helpers/whisper-cli
Helpers/whisper-dictate
